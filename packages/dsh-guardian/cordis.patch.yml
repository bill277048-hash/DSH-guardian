# dsh-guardian bundle patch。向插件树插入本插件行；独立安装场景下由
# scripts/deploy.sh 向 profile 层 cordis.patch.yml 幂等追加同 id 条目。
# id 命名与 profile 现有 web-ui-* 行一致。
- insert:
    - id: web-ui-guardian
      name: '@botton/dsh-guardian'
